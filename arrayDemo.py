import numpy as np

arr = np.array([10,20,30,40,50,60])
print(f"type: {type(arr)}, arr: {arr}")

from numpy import *
arr = array([40,80,20,30,70], int)
print(f"arr: {arr}")

carr = array(['a', 'b', 'c', 'd', 'e'])
print(f"carr: {carr}")

sarr = array(["python", "ETL", "Apache"], dtype=str)
print(f"sarr: {sarr}")